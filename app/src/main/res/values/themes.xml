<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Ledger" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- AndroidX Splash Screen launch theme. Used by MainActivity as its launch
         theme; installSplashScreen() switches to postSplashScreenTheme right
         after super.onCreate(). Works on all minSdk-26+ devices, so pre-12
         phones get the same splash as Android 12+ (no system fallback). -->
    <style name="Theme.Ledger.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Ledger</item>
    </style>
</resources>
