<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ uses the system SplashScreen automatically; these attributes
         pick the colour and icon. Pre-12 devices fall back to the regular window
         background — adequate at minSdk 26. -->
    <style name="Theme.Ledger" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
    </style>
</resources>
